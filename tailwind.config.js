module.exports = {
    purge: ["./src/**/*.svelte", "./public/**/*.html"],
    theme: {
        extend: {},
    },
    variants: {},
    plugins: [
        // require('@tailwindcss/ui') comment out for adding tailwind css
    ],
}